# Exclude automatic identification of unidentified tracks

Music Metadata Perfecter focuses on user-guided metadata repair for local tracks with at least one reliable identity clue. Tracks with no usable clue in embedded data, their filename, a Provider identifier, or user knowledge are outside the product's core scope because automatic audio identification would introduce a separate fingerprinting, candidate-disambiguation, confidence, and failure-handling workflow that would materially increase product complexity.
